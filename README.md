# Eval Poison Sentinel

**Lens:** evaluation integrity (Scale-class)  
**Innovation:** Detect exact train→eval example-ID overlap and exact supplied feature-signature overlap, bind dataset + policy identity into a deterministic contamination receipt, and fail closed above explicit thresholds.

This repository does **not** compute semantic similarity itself. A feature signature can represent a near-duplicate bucket only when an upstream process explicitly defines it that way.

It is complementary to `scale-label-collusion-bound` and `scale-agreement-budget-allocator`, but no sibling integration is claimed until executable receipt binding is proven.

## Claim ceiling (independent reference)

Independent GlacierEQ reference only. No company affiliation, employment, deployment,
endorsement, clearance, proprietary access, or production use is claimed. Names label public problem spaces.
